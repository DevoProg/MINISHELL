malloc empty pas proteger sur malloc                                        /!!!\

cat | ls ? en cours..                                                          /!!!\

make -n affiche pas les flags?    

var env _ = /Users/guixmoht99/Desktop/minis8/code/./minishell a la place de env on doit gerer?? /!!!\

tester le bash en heredoc comme la varsion bash ecole pour changer le prompt OK

fonction exit print exit avant de exit                                     /!!!\ OK

/bin/ls/unknown-> command not found au lieu de not a directory: /bin/ls/unknow il faut changer??

changer le message ctrl-D?                              OK

ls puis->control_C puis->enter affiche ls(chez moi en tout cas           /!!!\ OK

control backslash ne fonctionne pas   avec la commande cat               /!!!\ 

echo "cat lol.c | cat"
cat lol.c                                                               /!!!\ . OK

"ls | cat" ->execute commande et ne devrait pas

echo '$USER'
guixmoht99                                                              /!!!\ . OK

echo yo ">" test                                                           /!!!\ . OK

echo '$USER'                                                            /!!!\ OK

si guillemet dans fichier de redirection OK

inverser put_env_var et les redirections dans le main OK

tout free dans le bon ordre main OK

si trop de pipe ou trp de redirection erreur 258 OK

si redirection vers variable env qui existe pas code erreur 1

code erreur lorsque il y a pas de fichier apres la redirection

gerer code erreur des signaux

erreur 130 cat->control_C


control_c dans <<